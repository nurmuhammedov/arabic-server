node_modules
dist
.git
.env
.env.*
!.env.example

# The corpus is only read by the seed, which is run from a developer machine
# against the remote database rather than from inside the image.
src/database/seeds/data

*.log
.vscode
.idea
